@model IEnumerable<chuyende.Models.ChucVu>

@{
    ViewBag.Title = "Chức vụ";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="table-container">
    <h3>Danh sách chức vụ</h3>
   
    <div>
        @if (TempData["SuccessMessage"] != null)
        {
            <div class="custom-alert-success">
                @TempData["SuccessMessage"]
            </div>
        }
        @if (TempData["ErrorMessage"] != null)
        {
            <div class="custom-alert-danger">
                @TempData["ErrorMessage"]
            </div>
        }


    </div>

    <div class="actions">
        <a href="@Url.Action("Create", "ChucVus")" class="add-btn">
            <i class="fas fa-plus"></i> Thêm mới
        </a>
        <a href="@Url.Action("Trash", "ChucVus")" class="trash-btn">
            <i class="fas fa-trash"></i> Thùng rác
        </a>

    </div>

    <table>
        <thead>
            <tr>
                <th>#</th>
                <th>Mã chức vụ</th>
                <th>Tên chức vụ</th>
                <th>Chức năng</th>
            </tr>
        </thead>
        <tbody>
            @{ int stt = 1; }
            @foreach (var item in Model)
            {
                <tr>
                    <td>@stt</td>
                    <td>@Html.DisplayFor(modelItem => item.MaCV)</td>
                    <td>@Html.DisplayFor(modelItem => item.TenCV)</td>
                    <td class="action-icons">
                        <a href="@Url.Action("Edit","ChucVus", new { id = item.MaCV })" class="btn-icon edit">
                            <i class="fas fa-edit"></i>
                        </a>
                        <a href="@Url.Action("Details","ChucVus", new { id = item.MaCV })" class="btn-icon view">
                            <i class="fas fa-eye"></i>
                        </a>
                        @using (Html.BeginForm("MoveToTrash", "ChucVus", new { id = item.MaCV }, FormMethod.Post, new { @class = "delete-form" }))
                        {
                            @Html.AntiForgeryToken()
                            <button type="submit" class="btn-icon delete">
                                <i class="fas fa-trash"></i>
                            </button>
                        }
                    </td>


                </tr>
                stt++;
            }
        </tbody>
    </table>
</div>

